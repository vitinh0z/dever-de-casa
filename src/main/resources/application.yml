spring:
  application:
    name: dever-de-casa
  datasource:
    url: jdbc:postgresql://localhost:5432/deverdecasa
    username: deverdecasa
    password: deverdecasa
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080
